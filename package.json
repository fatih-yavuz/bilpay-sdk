{
  "name": "bilpay-sdk",
  "version": "1.0.0",
  "description": "Bilpay SDK for receiving payments",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && webpack",
    "tsc": "tsc -w",
    "types": "tsc -d --emitDeclarationOnly --allowJs false --checkJs false --declaration true --declarationDir ./@types",
    "prepublish": "npm run tsc && npm run types",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "bilpay"
  ],
  "author": "Fatih Yavuz",
  "license": "MIT",
  "dependencies": {
    "animate.css": "^3.7.0",
    "jquery": "^3.4.1",
    "micromodal": "^0.4.0",
    "modal": "^1.2.0",
    "qrcode": "^1.3.3"
  },
  "devDependencies": {
    "@types/jquery": "^3.3.29",
    "@types/micromodal": "^0.3.0",
    "webpack-cli": "^3.3.2"
  }
}
